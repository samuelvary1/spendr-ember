{{outlet}}

Expenses:
<ul>
  {{#each model as |expense|}}
  <li class="expenses">
    ${{expense.amount}} for {{expense.note}}
  </li>
   <button {{action 'delete' expense}} type="button" class="button alert tiny">Delete</button>
  {{/each}}
</ul>


<p>
	Total expenses: $0
</p>

<p>
	{{link-to "Add a new expense!" "expenses.new"}}
</p>
