

<div class="container">
  <div class="row">
    <ul class='list-inline'>
      <li>{{link-to "User" "users"}}</li>
      <li>{{link-to "Categories" "categories"}}</li>
      <li>{{link-to "Expenses" "expenses"}}</li>
      <li>{{link-to "Sign Up" "signup"}}</li>
      {{#if session.isAuthenticated}}
        <li><button class="btn btn-primary btn-sm" {{action "logout"}}>Logout</button></li>
      {{else}}
        <li>{{link-to 'Login' 'login'}}</li>
      {{/if}}
    </ul>
    <h4 id="title">Spendr - Here to help budget your spending.</h4>
  </div>
  {{outlet}}
</div>
